
const { airdropModel } = require("./airdropModel");
const { claimModel } = require("./claimModel");
const { agentModel } = require("./agentModel");
const { agentChatModel } = require("./agentChatModel");
const { agentTempChatModel } = require("./agentTempChatModel");







module.exports = {
    airdropModel,
    claimModel,
    agentModel,
    agentChatModel,
    agentTempChatModel
}